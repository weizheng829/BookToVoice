# 统一换行符：仓库内所有文本文件使用 LF，避免 Windows/macOS/Linux 跨平台冲突
* text=auto eol=lf

# 明确的二进制文件，不做换行符与 diff 处理
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.mp3  binary
*.wav  binary
*.db      binary
*.db-shm  binary
*.db-wal  binary
